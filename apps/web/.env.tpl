# 1Password template for apps/web. COMMITTED to git — no secrets here.
# Rendered to apps/web/.env by `npm run env:pull`.
# The web app currently only needs the API base URL (not secret).

VITE_API_BASE_URL=http://localhost:8000
