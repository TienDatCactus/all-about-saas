# 1Password template for packages/transactional. COMMITTED to git — no secrets.
# Rendered to packages/transactional/.env by `npm run env:pull`.

# Base URL of the API server where static email assets (logo.png, etc.) live.
BASE_URL=http://localhost:8000
